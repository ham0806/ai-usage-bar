# DESIGN

Windows 11 のタスクバーに常駐する使用量ウィジェット。既存の `Themes` を正本とし、色やフォントを他製品から持ち込まない。

## Design principles

- 対象ユーザー・利用場面：作業中にタスクバーだけで Cursor / Codex の残量感を見る
- 最も重要なユーザー操作：左クリックで詳細フライアウト、右クリックで更新・設定・終了
- 優先する価値・雰囲気：通知領域に溶ける静かな常駐。数字はすぐ読めて、説明はクリック後に出す
- 正本となる design system：`src/AiUsageBar.Core/Theme.cs` と Windows 11 のフライアウト（角丸、Mica）

## Visual hierarchy

- 主情報：プロバイダアイコンと、その時点で一番厳しい使用率（高いほど警告色）
- Codex の残り量とリセット時刻はフライアウトの内訳へ。バーの数字は使用率で揃える
- 補助情報：ホバー時のツールチップ（`Cursor 42%` 形式）と「クリックで詳細」
- 詳細：フライアウトの内訳、進捗、リセット、課金周期、最終更新
- 左クリックが主操作。右クリックは副操作

## Layout

- バー：アイコン + 短いパーセントを横に並べる。プロバイダ名や 5h/7d の併記は置かない
- フライアウト：プロバイダ見出し → 大きな主数値 → 内訳の進捗 → 補足行。プロバイダごとに薄い区切り線
- 各プロバイダを card で囲まない。入れ子のパネルも増やさない

## Spacing

- バー：左右パディング 8px、アイコンと数字の間 4px、プロバイダ間 10px（`WidgetLayout`）
- フライアウト：外側 16×14、プロバイダ間 12、内訳行のラベルとバーの間 4

## Typography

- バー：Segoe UI Variable Small / Segoe UI / Yu Gothic UI。パーセントは Regular、基準 12px
- アイコンはバー高さに合わせて 16〜24px。数字より少し大きくする
- フライアウト：Yu Gothic UI / Segoe UI Variable Text。見出し 13、主数値 22、本文 12、補足 11
- フライアウトの主数値はプロバイダにつき1つ。内訳側で巨大化しない

## Colors

- 背景・文字・境界・警告は `Themes.Dark` / `Themes.Light`。アクセントはシステムの AccentColor
- 使用率の色：70% 未満は Ok、70% 以上は Warn、90% 以上は Bad。エラーは Warn
- 進捗トラックはフライアウト背景と前景の Mix（前景 14%）で、塗りは使用率色

## Radius

- バーのホバー面 6、フライアウトは Win11 の角丸、進捗バー 2
- プロバイダ単位で角丸コンテナを増やさない

## Components

- WPF の `Border` / `StackPanel` / `Grid` のみ。追加の UI ライブラリは使わない
- アイコンは Cursor / Codex の識別にだけ使う。公式ロゴは同梱せず、利用者が置いた PNG を読む

## Responsive behavior

- バー高さはタスクバー厚みに追従（`WidgetLayout.Height`）
- フライアウトはタスクバー上に出し、画面外へはみ出したら上下を反転
- 対象はデスクトップのタスクバーのみ

## Interaction

- 通常：アイコンと主パーセントだけ
- ホバー：背景を少し明るく／暗くし、ツールチップで内訳
- 左クリック：詳細フライアウトをトグル。Esc とフォーカス喪失で閉じる
- 空：プロバイダなしの案内。エラー：短いラベルをバーに出し、理由はフライアウトへ

## Do / Don't

| Do | Don't |
| --- | --- |
| バーは一目の使用率に絞る | タスクバーに Auto/API や 5h/7d を並べる |
| 詳細はフライアウトの進捗と補足へ | 同じ数字を見出しと本文で重複して大きく出す |
| `Themes` の色と PercentColor を使う | 独自の虹色やグラデーションを足す |
| アイコンでプロバイダを区別する | バーに「Cursor」「Codex」の文字を足す |
